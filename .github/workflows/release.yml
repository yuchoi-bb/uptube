name: release

on:
  push:
    branches: ["claude/gotool-windows-release-l1clz1", "main", "master"]
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to create"
        default: "v0.1.0"
        required: true

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "stable"

      - name: Build
        run: go build -trimpath -ldflags "-s -w -H windowsgui" -o gotool.exe .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: gotool
          path: gotool.exe

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag || (startsWith(github.ref, 'refs/tags/') && github.ref_name) || 'v0.1.0' }}
          files: gotool.exe
